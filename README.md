# セミナー受付管理アプリ

スマホで片手操作できる、イベント受付管理アプリです。  
Supabaseをバックエンドに使用し、リアルタイムでデータが同期されます。

## 機能

- 🔒 **合言葉認証**: URLは公開可能。合言葉を知っている人だけがアクセス可能
- 📋 **参加者リスト**: 氏名・会社名を一覧表示
- ✅ **ワンタップ受付**: ボタン一つで受付完了。来場時刻を自動記録
- 🔄 **リアルタイム同期**: 複数端末でデータが即時反映
- 📱 **モバイル最適化**: 片手で操作しやすいUI

## セットアップ手順

### 1. Supabase プロジェクトの準備

1. [Supabase](https://supabase.com/) でプロジェクトを作成
2. **SQL Editor** で `supabase_setup.sql` を実行
3. **Authentication > Users** で共有ユーザーを作成:
   - Email: `staff@example.com` （任意）
   - Password: 合言葉として使用するパスワード

### 2. 環境変数の設定

`.env` ファイルを編集:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_SHARED_EMAIL=staff@example.com
```

> URLとAnon Keyは **Settings > API** から取得できます。

### 3. 開発サーバー起動

```bash
npm install
npm run dev
```

## デプロイ (GitHub Pages)

```bash
npm run build
```

`dist/` フォルダの内容をGitHub Pagesにデプロイしてください。

## 合言葉の変更方法

管理者（あなた）のみが変更可能です:

1. Supabase ダッシュボードにログイン
2. **Authentication > Users** を開く
3. 共有ユーザーを選択し、パスワードを変更

## 技術スタック

- React + Vite
- Tailwind CSS
- Supabase (Auth + Database + Realtime)
- Lucide React (アイコン)
